<%= form_with(model: garden, local: true) do |form| %>
  <% if garden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(garden.errors.count, "error") %> prohibited this garden from being saved:</h2>

      <ul>
      <% garden.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sizex %>
    <%= form.number_field :sizex, id: :garden_sizex %>
  </div>

  <div class="field">
    <%= form.label :sizey %>
    <%= form.number_field :sizey, id: :garden_sizey %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
